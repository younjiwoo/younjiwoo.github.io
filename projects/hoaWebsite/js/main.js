$(document).ready(function() {
// "When the document is ready, then do the functions inside the braces."
}); 
/* this is for jQuery (jQuery is a library). you have to use a different version of that if you only use javascript. window.onload={code in here} */